<div class="card">
  <div class="card-header">
    <%= "tag #{@tag.name} transactions" %>
  </div>

  <div class="card-body">
    <%= render partial: 'orders/list', locals: { objects: @transactions } %>
  </div>
</div>